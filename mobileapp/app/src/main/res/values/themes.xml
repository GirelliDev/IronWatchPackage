<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.IronWatch" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cores principais -->
        <item name="colorPrimary">@color/purple_40</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/purple_80</item>
        <item name="colorOnPrimaryContainer">@color/black</item>

        <item name="colorSecondary">@color/teal_40</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Estes aqui estavam errados -->
        <!-- CORRIGIDO: usar android:colorBackground, não attr/colorBackground -->
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>

        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
    </style>
</resources>
